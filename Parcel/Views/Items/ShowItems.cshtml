@using Parcel.Models;
<h1> Your Parcel.</h1>

<li>Size: @Model.GetWidth() X @Model.GetHeight() X @Model.GetLength()</li>
<li>Volume: @Model.Volume()</li>
<li>Cost to Ship: @Model.CostToShip()</li>

<a href="/parcels/new">Create a new parcel.</a>
